---
driver:
  name: vagrant

driver_config:
  require_chef_omnibus: true
provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-14.04
  - name: centos-6.4

suites:
  - name: default
    run_list:
      - recipe[memcached::default]
    attributes:
